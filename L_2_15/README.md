# Unix Shell Interpreter в Go

Полнофункциональный интерпретатор Unix shell, написанный на Go. Поддерживает встроенные команды, конвейеры, условное выполнение, переменные окружения и редиректы файлов.

## 🎯 Требования задачи

### Обязательный функционал
- ✅ **Встроенные команды**: `cd`, `pwd`, `echo`, `kill`, `ps`, `exit`
- ✅ **Запуск внешних команд** через `os/exec`
- ✅ **Конвейеры (pipelines)**: объединение команд через `|`
- ✅ **Обработка завершения**: 
  - `Ctrl+D` (EOF) - выход из shell
  - `Ctrl+C` - прерывание текущей команды

### Дополнительный функционал
- ✅ **Условное выполнение**: `&&` и `||`
- ✅ **Переменные окружения**: подстановка `$VAR` и `${VAR}`
- ✅ **Редиректы**: `>`, `>>`, `<` для работы с файлами

## 🚀 Быстрый старт

### Компиляция

```bash
cd L_2_15
go build -o shell
```

### Запуск интерактивно

```bash
./shell
```

Вы увидите приглашение вида:
```
[hostname] ~ $ 
```

### Запуск скрипта

```bash
echo "echo hello
ps | head -5
cat > output.txt <<EOF
test
EOF" | ./shell
```

## 🔧 Встроенные команды

### `cd <path>` - Смена директории

```bash
$ cd /tmp              # Абсолютный путь
$ cd ..                # Относительный путь
$ cd ~                 # Домашняя директория
$ cd                   # Без аргументов → домашняя директория
```

**Особенности:**
- Поддержка `~` для домашней директории
- Относительные пути разрешаются от текущей директории
- Обновляет переменную `$PWD`

### `pwd` - Текущая директория

```bash
$ pwd
/current/working/directory
```

### `echo <args>` - Вывод текста

```bash
$ echo hello world
hello world

$ echo $HOME
/home/username

$ echo "text" > file.txt  # С редиректом
```

**Особенности:**
- Поддержка переменных окружения
- Работает в конвейерах
- Поддерживает редиректы

### `ps [args]` - Список процессов

```bash
$ ps              # То же, что ps aux
$ ps aux          # С аргументами
$ ps | grep bash  # В конвейере
```

**Особенности:**
- По умолчанию выполняет `ps aux`
- Работает в конвейерах
- Приказывает к системной команде `ps`

### `kill <pid> [signal]` - Отправить сигнал процессу

```bash
$ kill 1234            # SIGTERM (15)
$ kill 1234 9          # SIGKILL (9)
$ kill 1234 15         # SIGTERM (15)
```

**Сигналы:**
- `9` - SIGKILL (безусловное завершение)
- `15` - SIGTERM (корректное завершение, по умолчанию)
- Другие номера сигналов из `syscall`

### `exit` - Завершить shell

```bash
$ exit
```

## 🔗 Конвейеры (Pipelines)

Объединение команд через `|` для цепочки обработки данных:

```bash
$ ps aux | grep bash
$ echo "hello world" | wc -c
$ cat file.txt | grep pattern | wc -l
```

**Как работает:**
1. Создается pipe между каждой парой команд
2. stdout первой команды → stdin второй
3. Все команды запускаются параллельно
4. stdout последней команды идет в консоль

**Ограничения:**
- Встроенные команды (`cd`, `pwd`, `kill`, `exit`) **не могут быть в конвейере**
- `echo` и `ps` работают в конвейерах как внешние команды

## 🔀 Условное выполнение

### `&&` - И (AND)

Вторая команда выполняется только если первая успешна (exit code = 0):

```bash
$ echo "success" && echo "next"     # Выведет обе строки
$ false && echo "not shown"          # Не выведет вторую строку
```

### `||` - Или (OR)

Вторая команда выполняется только если первая не успешна (exit code ≠ 0):

```bash
$ false || echo "executed"           # Выведет вторую строку
$ true || echo "not shown"           # Не выведет вторую строку
```

### Комбинирование

```bash
$ cmd1 && cmd2 || cmd3              # Сложные условия
```

## 🌍 Переменные окружения

Подстановка переменных окружения перед выполнением команды:

### Синтаксис `$VAR`

```bash
$ export HOME="/home/user"
$ echo $HOME
/home/user

$ cd $HOME
```

### Синтаксис `${VAR}`

```bash
$ export NAME="John"
$ echo Hello ${NAME}!
Hello John!
```

### В конвейерах

```bash
$ export PATTERN="bash"
$ ps aux | grep $PATTERN
```

### Встроенные переменные

```bash
$ echo $HOME            # Домашняя директория
$ echo $PWD             # Текущая директория
$ echo $USER            # Текущий пользователь
$ echo $PATH            # Путь поиска команд
```

## 📄 Редиректы файлов

### `>` - Перезапись в файл

Перенаправить stdout в файл, перезаписав его содержимое:

```bash
$ echo "hello" > output.txt
$ ls -la > listing.txt
```

### `>>` - Добавление в файл

Перенаправить stdout в файл, добавив в конец:

```bash
$ echo "line 1" > file.txt
$ echo "line 2" >> file.txt   # Теперь в файле две строки
```

### `<` - Чтение из файла

Перенаправить stdin из файла:

```bash
$ cat < input.txt
$ grep "pattern" < file.txt
```

### Комбинирование с конвейерами

```bash
$ ps | grep bash > bash_processes.txt
$ cat < input.txt | wc -l
```

## 📊 Архитектура

### Основные компоненты

#### `Shell` структура

```go
type Shell struct {
    reader *bufio.Reader    // Буферизованный ввод
    writer io.Writer        // Вывод (обычно os.Stdout)
    cwd    string          // Текущая рабочая директория
    exit   bool            // Флаг завершения
}
```

#### Основной цикл (`Run()`)

```
1. Вывод приглашения
   ↓
2. Чтение строки команды
   ↓
3. Проверка EOF (Ctrl+D) → выход
   ↓
4. Выполнение команды
   ↓
5. Проверка флага выхода
   ↓
(повторить с шага 1)
```

### Обработка команды

```
Входная строка
   ↓
parseConditionals() → разбор && и ||
   ↓
executePipeline() → разбор |
   ↓
parseRedirects() → разбор >, <, >>
   ↓
parseCommand() → создание exec.Cmd
   ↓
applyRedirects() → подключение файлов
   ↓
cmd.Run() или cmd.Start() + cmd.Wait()
   ↓
Возврат exit code
```

### Функции парсинга

| Функция | Задача | Входные данные | Выходные данные |
|---------|--------|----------------|-----------------|
| `parseConditionals()` | Разбор `&&` и `\|\|` | Строка команды | Слайс команд, операторы |
| `parseRedirects()` | Разбор `>`, `>>`, `<` | Строка команды | Команда, структура Redirect |
| `parseCommand()` | Создание exec.Cmd | Строка команды | *exec.Cmd или nil |
| `executePipeline()` | Выполнение конвейера | Строка команды | Exit code |
| `executeSingleCommand()` | Выполнение одной команды | Строка команды | Exit code |
| `executeBuiltin()` | Выполнение встроенной команды | Слайс аргументов | bool |

## 🧪 Тестирование

### Запуск всех тестов

```bash
go test -v
```

### Запуск конкретных тестов

```bash
# Встроенные команды
go test -v -run TestBuiltin

# Переменные окружения
go test -v -run EnvVar

# Редиректы
go test -v -run Redirect

# Конвейеры
go test -v -run Pipeline
```

## 💡 Примеры использования

### Простые команды

```bash
$ echo "Hello, World!"
Hello, World!

$ pwd
/home/user

$ cd /tmp
$ pwd
/tmp
```

### Конвейеры

```bash
$ ls -la | grep ".go"
$ ps aux | grep bash | wc -l
$ cat file.txt | sort | uniq
```

### Условное выполнение

```bash
$ mkdir newdir && cd newdir && pwd
/home/user/newdir

$ rm nonexistent.txt || echo "File not found"
File not found
```

### Переменные окружения

```bash
$ export LOGDIR="/var/log"
$ ls $LOGDIR > $LOGDIR/listing.txt
```

### Редиректы

```bash
# Создать файл
$ echo "line 1" > myfile.txt

# Добавить в файл
$ echo "line 2" >> myfile.txt

# Прочитать из файла
$ cat < myfile.txt
line 1
line 2

# Комбинация с конвейером
$ ps aux | grep bash > bash_processes.txt
```

### Сложные команды

```bash
$ echo "test content" > input.txt && cat < input.txt | wc -c && echo "Done"
```